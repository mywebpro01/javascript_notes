{
  "name": "json-server-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx json-server --watch db.json --host 0.0.0.0 --port $PORT --static ./public"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
